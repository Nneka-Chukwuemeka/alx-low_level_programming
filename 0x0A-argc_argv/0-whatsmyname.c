#include <stdio.h>

/**
 * main - A function that prints the argument the filename
 * @argc: An argument that count number of argument
 * @argv: A pointer of array
 * Return: Success is zero
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf(" %s \n", argv[0]);
	return (0);
}	
