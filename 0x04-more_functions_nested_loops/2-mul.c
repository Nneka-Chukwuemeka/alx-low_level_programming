#include "main.h"

/**
 * mul - multiply two numbers and print it
 * @a: first parameter
 * @b: second parameter
 * Return: Return 1 when its true
*/
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
