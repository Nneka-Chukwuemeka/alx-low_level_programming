#include "main.h"
/**
* _islower - print the ones that are lower case
* @c: parameter of _islower
* Return: Always 1 (Success)
*/
int _islower(int c)
{
	int result;

	if (c >= 'a' && c <= 'z')
	{
		c = result;
	}
	return (result);
}
