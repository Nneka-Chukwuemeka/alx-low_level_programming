#include <stdio.h>

/**
 * main - A functions that counts number of arguments
 * @argc: The parameter that prints argument number
 * @argv: An undefined parameter
 *
 * Return: 0 in success
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
